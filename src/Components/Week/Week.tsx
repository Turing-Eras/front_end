import React, { useEffect, useState } from 'react';
import './Week.css';

const Week = () => {
  let handleClick = () => {
    
  };
  return <section className='week' onClick={handleClick}></section>;
};

export default Week;
